#include<stdio.h>
/**
 * _putchar - using a putchar function
 * Description: Writing a putchar to stdout
 * Return: 0
 */
int _putchar(char c)
{
	int i = 0, temp;
	c = i;
	temp = c;
	i = temp;

	return (i);
}
