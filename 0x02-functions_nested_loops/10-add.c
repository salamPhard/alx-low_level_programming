#include "main.h"
/**
 * add - adding two integers
 * @a: first parameter
 * @b: second parameter
 *
 * Description: adding two integers and calling
 * Return: 0;
 */
int add(int a, int b)
{
	int total = a + b;

	return (total);
}
